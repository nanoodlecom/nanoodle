#!/usr/bin/env node
// Render the static, first-party output review page. No network or model calls.
import {readFileSync,writeFileSync,existsSync} from 'node:fs';
import {resolve,dirname,join} from 'node:path';
import {fileURLToPath} from 'node:url';
import {createHash} from 'node:crypto';
import {gzipSync} from 'node:zlib';
const root=resolve(dirname(fileURLToPath(import.meta.url)),'../examples/gallery');
const samples=JSON.parse(readFileSync(join(root,'samples.json'),'utf8'));
const esc=s=>String(s).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const local=file=>{if(!/^[\w/-]+\.[\w]+$/.test(file)||file.includes('..')||!existsSync(join(root,file)))throw Error('Missing/invalid sample asset: '+file);return esc(file);};
const sections=samples.map(s=>{
 const graph=readFileSync(join(root,local(s.slug+'/graph.json')));
 if(createHash('sha256').update(graph).digest('hex')!==s.graphSha256)throw Error('Sample graph changed: '+s.slug);
 const workflow=readFileSync(join(root,local(s.workflow)));
 if(createHash('sha256').update(workflow).digest('hex')!==s.workflowSha256)throw Error('Workflow changed: '+s.slug);
 const link='https://nanoodle.com/#g='+gzipSync(workflow).toString('base64url');
 const outputs=s.outputs.map(o=>`<figure>${o.kind==='text'?`<blockquote>${esc(readFileSync(join(root,local(o.src)),'utf8'))}</blockquote>`:o.kind==='video'?`<video controls preload="none" poster="${local(s.preview)}" aria-label="${esc(s.title)}"><source src="${local(o.src)}" type="video/mp4"></video>`:o.kind==='audio'?`<audio controls preload="none" aria-label="${esc(s.title)}"><source src="${local(o.src)}" type="audio/mpeg"></audio>`:`<a href="${local(o.src)}"><img src="${local(o.src)}" alt="${esc(o.label+' — '+s.title)}" loading="lazy"></a>`}<figcaption>${esc(o.label)} · <a href="${local(o.src)}" download>Download</a></figcaption></figure>`).join('\n');
 const inputs=s.inputs.map(i=>i.src?`<figure><img src="${local(i.src)}" alt="${esc(i.label)}" loading="lazy"><figcaption>${esc(i.label)}</figcaption></figure>`:`<div><h4>${esc(i.label)}</h4><p>${esc(i.text)}</p></div>`).join('\n');
 return `<section id="${esc(s.slug)}"><div class="heading"><span>${esc(s.review)} · ${esc(s.date)}</span><h2>${esc(s.title)}</h2></div><p>${esc(s.note)}</p><div class="outputs ${s.outputs.length>1?'comparison':''}">${outputs}</div><p class="meta">Reported run cost: ${s.costExact?'':'at least '}$${Number(s.costUsd.toFixed(5))}. Models: ${s.models.map(esc).join(', ')}. Prices and results vary.</p><details><summary>Input brief and source images</summary><div class="inputs">${inputs}</div></details><p class="links"><a href="${link}">Open workflow ↗</a><a href="/guide/examples/${esc(s.slug)}">How to use this</a><a href="${local(s.workflow)}" download>Download workflow</a><a href="${local(s.slug+'/graph.json')}" download>Original sampled graph</a><a href="https://mcp.nanoodle.com/#${esc(s.slug)}">Explore MCP tools ↗</a></p></section>`;
}).join('\n');
const html=`<!doctype html>
<html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>Workflow samples — nanoodle</title><meta name="description" content="Actual nanoodle workflow outputs, with the input brief, model choices, observed costs and review notes."><link rel="canonical" href="https://nanoodle.com/examples/gallery/"><style>
:root{color-scheme:light;--ink:#182b28;--muted:#53655f;--line:#d9ded5;--paper:#f7f7f0;--accent:#24624b}*{box-sizing:border-box}body{margin:0;background:var(--paper);color:var(--ink);font:16px/1.65 system-ui,sans-serif}main{max-width:1040px;margin:auto;padding:40px 24px 80px}a{color:var(--accent);text-underline-offset:3px}header{padding:24px 0 38px;border-bottom:1px solid var(--line)}header>a{font-weight:750;text-decoration:none}h1{font-size:clamp(34px,6vw,62px);line-height:1.06;letter-spacing:-2px;margin:30px 0 20px}header p{max-width:760px}nav{display:flex;flex-wrap:wrap;gap:8px 20px;margin-top:24px}nav a{font-size:14px}section{padding:40px 0;border-bottom:1px solid var(--line);scroll-margin-top:20px}h2{font-size:30px;line-height:1.2;margin:8px 0 18px;letter-spacing:-.7px}.heading span,.meta,figcaption{font-size:13px;color:var(--muted)}figure{margin:0;min-width:0}figure img,video{display:block;width:100%;max-height:620px;object-fit:contain;background:#e8eae2;border-radius:10px}audio{width:100%}figcaption{margin:8px 0 18px}.outputs{max-width:760px;margin:24px 0}.comparison{display:grid;grid-template-columns:1fr 1fr;gap:20px;max-width:none}.comparison img{aspect-ratio:1;object-fit:contain}blockquote{margin:0;padding:24px;border-left:3px solid var(--accent);background:white;white-space:pre-wrap}.inputs{display:grid;grid-template-columns:repeat(auto-fit,minmax(240px,1fr));gap:24px;padding:20px 0}.inputs img{max-height:280px}.inputs h4{margin:0}.inputs p{white-space:pre-wrap;font-size:14px}summary{cursor:pointer;font-weight:600}.links{display:flex;flex-wrap:wrap;gap:12px 24px;font-size:14px}@media(max-width:600px){main{padding:20px 18px 60px}.comparison{grid-template-columns:1fr}h1{letter-spacing:-1px}section{padding:30px 0}}
</style></head><body><main><header><a href="/">nanoodle 🍜</a><h1>See what a workflow produces.</h1><p>Actual first-party runs with the input brief, selected models and review notes. Open a sample graph to reuse it. Viewing these saved outputs is free; generating your own uses your NanoGPT balance.</p><p>Image and text samples were inspected; video notes describe sampled frames. Audio review is labeled separately. These examples show observed results, including limitations, rather than guaranteeing the next generation.</p><p>Want to remix one? The <a href="/guide/examples/">examples how-to</a> walks through each workflow: which inputs to edit, what to inspect, and the reported cost.</p><nav aria-label="Workflow samples"><a href="../iron-verdict/">Play Iron Verdict</a><a href="/guide/examples/">How to use each workflow</a>${samples.map(s=>`<a href="#${esc(s.slug)}">${esc(s.title)}</a>`).join('')}</nav></header>${sections}<footer><p>Made by nanoodle. No analytics. <a href="samples.json">Review manifest</a> · <a href="https://github.com/nanoodlecom/awesome-noodles">Workflow gallery</a></p></footer></main></body></html>\n`;
const target=join(root,'index.html');
if(process.argv.includes('--check')){if(!existsSync(target)||readFileSync(target,'utf8')!==html)throw Error('Sample page stale: run node scripts/sync-gallery-samples.mjs');}
else writeFileSync(target,html);
console.log(`Verified ${samples.length} sample graphs and assets${process.argv.includes('--check')?'':'; rendered gallery page'}`);
