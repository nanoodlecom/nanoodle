#!/usr/bin/env node
// Render the static, first-party output review page. No network or model calls.
import {readFileSync,writeFileSync,existsSync} from 'node:fs';
import {resolve,dirname,join} from 'node:path';
import {fileURLToPath} from 'node:url';
import {createHash} from 'node:crypto';
import {gzipSync} from 'node:zlib';
const root=resolve(dirname(fileURLToPath(import.meta.url)),'../examples/gallery');
const samples=JSON.parse(readFileSync(join(root,'samples.json'),'utf8'));
const esc=s=>String(s).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const local=file=>{if(!/^[\w/-]+\.[\w]+$/.test(file)||file.includes('..')||!existsSync(join(root,file)))throw Error('Missing/invalid sample asset: '+file);return esc(file);};
const sections=samples.map(s=>{
 const graph=readFileSync(join(root,local(s.slug+'/graph.json')));
 if(createHash('sha256').update(graph).digest('hex')!==s.graphSha256)throw Error('Sample graph changed: '+s.slug);
 const workflow=readFileSync(join(root,local(s.workflow)));
 if(createHash('sha256').update(workflow).digest('hex')!==s.workflowSha256)throw Error('Workflow changed: '+s.slug);
 const link='https://nanoodle.com/#g='+gzipSync(workflow).toString('base64url');
 const cover=s.preview && s.outputs.every(o=>o.kind==='audio'||o.kind==='text')?`<figure><a href="${local(s.preview)}"><img src="${local(s.preview)}" alt="${esc(s.title)}" loading="lazy"></a><figcaption>Cover still · <a href="${local(s.preview)}" download>Download</a></figcaption></figure>\n`:'';
 const outputs=cover+s.outputs.map(o=>`<figure>${o.kind==='text'?`<blockquote>${esc(readFileSync(join(root,local(o.src)),'utf8'))}</blockquote>`:o.kind==='video'?`<video controls preload="none" poster="${local(s.preview)}" aria-label="${esc(s.title)}"><source src="${local(o.src)}" type="video/mp4"></video>`:o.kind==='audio'?`<audio controls preload="none" aria-label="${esc(s.title)}"><source src="${local(o.src)}" type="audio/mpeg"></audio>`:`<a href="${local(o.src)}"><img src="${local(o.src)}" alt="${esc(o.label+' — '+s.title)}" loading="lazy"></a>`}<figcaption>${esc(o.label)} · <a href="${local(o.src)}" download>Download</a></figcaption></figure>`).join('\n');
 const proof=s.proof&&s.proof.images&&s.proof.images.length?`<h3>${esc(s.proof.heading||'Detector proof')}</h3>${s.proof.note?`<p>${esc(s.proof.note)}</p>`:''}<div class="outputs comparison proof">${s.proof.images.map(img=>`<figure><a href="${local(img.src)}"><img src="${local(img.src)}" alt="${esc(img.label+' — '+s.title)}" loading="lazy"></a><figcaption>${esc(img.label)} · <a href="${local(img.src)}" download>Download</a></figcaption></figure>`).join('\n')}</div>`:'';
 const inputs=s.inputs.map(i=>i.src?`<figure><img src="${local(i.src)}" alt="${esc(i.label)}" loading="lazy"><figcaption>${esc(i.label)}</figcaption></figure>`:`<div><h4>${esc(i.label)}</h4><p>${esc(i.text)}</p></div>`).join('\n');
 return `<section id="${esc(s.slug)}"><div class="heading"><span>${esc(s.review)} · ${esc(s.date)}</span><h2>${esc(s.title)}</h2></div><p>${esc(s.note)}</p><div class="outputs ${s.outputs.length>1?'comparison':''}">${outputs}</div>${proof}<p class="meta">Reported run cost: ${s.costExact?'':'at least '}$${Number(s.costUsd.toFixed(5))}. Models: ${s.models.map(esc).join(', ')}. Prices and results vary.</p><details><summary>Input brief and source images</summary><div class="inputs">${inputs}</div></details><p class="links"><a href="${link}">Open workflow ↗</a><a href="/guide/examples/${esc(s.slug)}">Steal this</a><a href="${local(s.workflow)}" download>Download workflow</a><a href="${local(s.slug+'/graph.json')}" download>Original sampled graph</a><a href="https://mcp.nanoodle.com/#${esc(s.slug)}">Explore MCP tools ↗</a></p></section>`;
}).join('\n');
// Dark panel voice — same tokens as guide/guide.css / the editor. Do not revert to paper/light.
const css=`:root{color-scheme:dark;--bg:#0b0d12;--panel:#12151d;--panel2:#171b25;--ink:#eef1f7;--dim:#9aa3b2;--muted:var(--dim);--line:#262c3a;--accent:#7c8cff;--accent2:#ff79c6;--cyan:#67e8f9}*{box-sizing:border-box}body{margin:0;background:radial-gradient(1100px 540px at 70% -10%,#1a1f3a55,transparent),var(--bg);color:var(--ink);font:16px/1.65 system-ui,-apple-system,Segoe UI,Roboto,sans-serif}main{max-width:1040px;margin:auto;padding:40px 24px 80px}a{color:var(--accent);text-decoration:none;text-underline-offset:3px}a:hover{text-decoration:underline}header{padding:24px 0 38px;border-bottom:1px solid var(--line)}header>a{font-weight:800;letter-spacing:-.02em;text-decoration:none;color:var(--ink)}header>a:hover{color:var(--cyan);text-decoration:none}h1{font-size:clamp(34px,6vw,62px);line-height:1.06;letter-spacing:-2px;margin:30px 0 20px;font-weight:850;background:linear-gradient(90deg,var(--cyan),var(--accent) 55%,var(--accent2));-webkit-background-clip:text;background-clip:text;color:transparent}header p{max-width:760px;color:#cdd3df}section>p{color:#cdd3df}nav{display:flex;flex-wrap:wrap;gap:8px;margin-top:24px}nav a{font-size:14px;border:1px solid var(--line);background:var(--panel2);color:var(--ink);padding:.32rem .7rem;border-radius:2rem}nav a:hover{border-color:#3a425a;text-decoration:none}section{padding:40px 0;border-bottom:1px solid var(--line);scroll-margin-top:20px}h2{font-size:30px;line-height:1.2;margin:8px 0 18px;letter-spacing:-.7px;font-weight:800}.heading span,.meta,figcaption{font-size:13px;color:var(--muted)}figure{margin:0;min-width:0}figure img,video{display:block;width:100%;max-height:620px;object-fit:contain;background:var(--panel);border:1px solid var(--line);border-radius:10px}audio{width:100%}figcaption{margin:8px 0 18px}.outputs{max-width:760px;margin:24px 0}.comparison{display:grid;grid-template-columns:1fr 1fr;gap:20px;max-width:none}.comparison img{aspect-ratio:1;object-fit:contain}.comparison.proof img{aspect-ratio:auto}h3{font-size:20px;margin:28px 0 10px;letter-spacing:-.3px}blockquote{margin:0;padding:24px;border-left:3px solid var(--accent);background:var(--panel2);border-radius:0 .6rem .6rem 0;white-space:pre-wrap;color:#dbe2ef}.inputs{display:grid;grid-template-columns:repeat(auto-fit,minmax(240px,1fr));gap:24px;padding:20px;margin-top:12px;background:var(--panel2);border:1px solid var(--line);border-radius:.7rem}.inputs img{max-height:280px}.inputs h4{margin:0;color:var(--ink)}.inputs p{white-space:pre-wrap;font-size:14px;color:#cdd3df}summary{cursor:pointer;font-weight:600;color:var(--ink)}.links{display:flex;flex-wrap:wrap;gap:12px 24px;font-size:14px}footer{color:var(--dim);font-size:.82rem;padding-top:1.1rem}@media(max-width:600px){main{padding:20px 18px 60px}.comparison{grid-template-columns:1fr}h1{letter-spacing:-1px}section{padding:30px 0}}`;
const html=`<!doctype html>
<html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>Workflow samples — nanoodle</title><meta name="description" content="Actual nanoodle workflow outputs, with the input brief, model choices, observed costs and review notes."><meta name="theme-color" content="#0b0d12"><link rel="canonical" href="https://nanoodle.com/examples/gallery/"><style>
${css}
</style></head><body><main><header><a href="/">nanoodle 🍜</a><h1>See what a workflow produces.</h1><p>Actual first-party runs with the input brief, selected models and review notes. Open a sample graph to reuse it. Viewing these saved outputs is free; generating your own uses your NanoGPT balance.</p><p>Image and text samples were inspected; video notes describe sampled frames. Audio review is labeled separately. These examples show observed results, including limitations, rather than guaranteeing the next generation.</p><p>Like what you see? <a href="/guide/examples/">Steal a noodle</a> — open the graph, change a line, hit run.</p><nav aria-label="Workflow samples"><a href="../iron-verdict/">Play Iron Verdict</a><a href="/guide/examples/">Steal a noodle</a>${samples.map(s=>`<a href="#${esc(s.slug)}">${esc(s.title)}</a>`).join('')}</nav></header>${sections}<footer><p>Made by nanoodle. No analytics. <a href="samples.json">Review manifest</a> · <a href="https://github.com/nanoodlecom/awesome-noodles">Workflow gallery</a></p></footer></main></body></html>\n`;
const target=join(root,'index.html');
if(process.argv.includes('--check')){if(!existsSync(target)||readFileSync(target,'utf8')!==html)throw Error('Sample page stale: run node scripts/sync-gallery-samples.mjs');}
else writeFileSync(target,html);
console.log(`Verified ${samples.length} sample graphs and assets${process.argv.includes('--check')?'':'; rendered gallery page'}`);
