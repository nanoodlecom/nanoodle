Cover still is job-describing card art via NanoGPT flux-schnell/ideogram; not paid QC output. Upload placeholders only, no fake driven thumb. no paid `wan-22-animate-2` QC. Job is still + driver → Volt motion transfer @480p (~$0.04/s; ~2–3s ≈ $0.08–0.12). Distinct from prompt-i2v (photo-to-video / grok-imagine-still / wan-still-audio), InfiniteTalk / talking-avatar, H3 Max multi-angle, and P-Video rewrite. Open the graph — upload a still + a short driver; Driven clip pins `wan-22-animate-2` @ 480p. No committed media in-repo.
